{
  "name": "protractor-cucumber-bdd",
  "version": "1.0.0",
  "description": "project with use of protractor and cucumber",
  "main": "conf.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "bart",
  "license": "ISC",
  "devDependencies": {
    "grunt": "^1.0.1"
  }
}
